<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("./partials/head.ejs") %>
    <title>Document</title>
</head>
<body>
    <%- include("./partials/nav.ejs") %>

    <div class="container mt-3">
        <h2><%= blogs.title %></h2>
        <img src="<%= blogs.ImageUrl %>" width="500px">
        <h3><%= blogs.body %></h3>
    </div>

    <div class="container">
        <img src="<%= blogs.createdBy.profileImageUrl %>" width="75px" ><%= blogs.createdBy.fullname %>
    </div>


   <div class="container">
    <h3>comments</h3>
   
        <form action="/blog/comment/<%= blogs._id %>" method="post">
            <div class="mt-3">
                <input type="text" name="content" placeholder="enter your comments">
                <button class="btn btn-sm btn-primary">post</button>
            </div>
         </form>
   </div>
   

    <%- include("./partials/script.ejs") %>
</body>
</html>